# case35 财报 RAG 全量依赖
# 安装：pip install -r requirements.txt

# --- 数据管线 ---
pypdf>=6.14            # step0 PDF 页数/拆分
requests>=2.32         # step1 MinerU API 调用
PyYAML>=6.0            # prompts.yaml / 配置加载
tiktoken>=0.13         # token 计数（chunking / 会话记忆压缩）

# --- 检索 ---
pymilvus>=3.0          # step5 向量入库 + 在线向量检索
rank-bm25>=0.2.2       # step6 BM25 索引 + 在线 BM25 检索
jieba>=0.42            # BM25 中文分词

# --- LLM / 结构化输出 ---
openai>=2.52           # OpenAI 兼容客户端（Agicto：chat / embedding / rerank）

# --- 问答服务 ---
fastapi>=0.141
uvicorn>=0.53
pydantic>=2.0          # 请求/响应模型

# --- 数据处理管线编排（scripts/pipeline_graph.py）---
langgraph>=1.2
